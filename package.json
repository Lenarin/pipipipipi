{
  "name": "after-rain-batumi",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:browser": "playwright test"
  },
  "dependencies": { "phaser": "4.2.1" },
  "devDependencies": { "@playwright/test": "^1.55.0", "typescript": "~5.9.3", "vite": "8.2.2", "vitest": "5.0.0" },
  "engines": { "node": ">=22.12.0" }
}
